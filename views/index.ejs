<!DOCTYPE html>
<html>
<head>
  <title>Bincom Test - Homepage</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css">
</head>
<body class="bg-light">

<div class="container py-5">
  <h1 class="text-center mb-4">🗳️ Bincom Test Application</h1>

  <div class="list-group">
    <a href="/" class="list-group-item list-group-item-action">View Polling Unit Results</a>
    <a href="/lga" class="list-group-item list-group-item-action">View Summed LGA Results</a>
    <a href="/addNew-result" class="list-group-item list-group-item-action">Add New Result</a>
  </div>

  <hr>
  <h3 class="mt-4">Select Polling Unit</h3>
  <form method="get" action="/polling-unit/<%= pollingUnits[0].uniqueid %>">
    <div class="row g-2">
      <div class="col-md-8">
        <select class="form-select" name="pu" onchange="this.form.action='/polling-unit/'+this.value">
          <% pollingUnits.forEach(pu => { %>
            <option value="<%= pu.uniqueid %>"><%= pu.polling_unit_name %></option>
          <% }) %>
        </select>
      </div>
      <div class="col-md-4">
        <button class="btn btn-primary w-100">View Results</button>
      </div>
    </div>
  </form>
</div>

</body>
</html>
